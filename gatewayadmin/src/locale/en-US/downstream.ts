export default {
  'downstream.scheme': 'Downstream Scheme',
  'downstream.http.version': 'Downstream Http Version',
  'downstream.http.method': 'Downstream Http Method',
  'downstream.path.template': 'Downstream Path Template'
}
